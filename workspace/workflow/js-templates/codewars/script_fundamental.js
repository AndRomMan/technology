'use strict';




// 1 Hello, Name or World!
// Возвращаем или Hello, World! или Hello, Имя! - имя с заглавной, остальные символы с маленькой1
/* let userName = prompt('input name', 'NAME');
function hello(name) {
  let result = name ? (name[0].toUpperCase() + name.slice(1).toLowerCase()) : 'World';
  return ('Hello, ' + result + '!');
}
console.log(hello(userName));
 */
